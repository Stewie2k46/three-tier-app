pipeline {
    agent any
    environment {
        DOCKER_CREDENTIALS_ID = 'jenkins-docker'  // Docker credentials ID from Jenkins credentials
    }
    stages {
        stage('Build and Push Docker Images') {
            steps {
                script {
                    // Ensure the build_push.sh script is executable
                    dir('jenkins') {
                        sh 'chmod +x build_push.sh'  // This sets the script as executable
                        sh './build_push.sh'
                    }
                }
            }
        }
        stage('Deploy to Minikube') {
            steps {
                script {
                    // Deploy backend
                    sh 'kubectl apply -f backend/deployment.yaml'

                    // Deploy frontend
                    sh 'kubectl apply -f frontend/deployment.yaml'

                    // Deploy database
                    sh 'kubectl apply -f database/deployment.yaml'
                }
            }
        }
    }
}
